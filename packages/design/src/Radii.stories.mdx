import { Meta } from "@storybook/addon-docs";

<Meta title="Design/Radii" />

# Border Radius

We favour a subtle rounding of corners wherever possible as a nod to the blunt
visual style of our marketing properties to afford a smoother transition from
the signup flow into the product. To achieve this appearance, use
`--radius-base`.

This base radius can be found in almost all of our components that have a
defined "edge": <a href="../?path=/docs/components-button--button">Button</a>,
<a href="../?path=/docs/components-card--card">Card</a>,
<a href="../?path=/docs/components-inputtext--input-text">InputText</a>, and
<a href="../?path=/docs/components-menu--menu">Menu</a> are all examples of this
in practice.

Larger rounded corners should be used sparingly, but are available for cases
where the user may expect a smoother corner, such as
<a href="../?path=/docs/components-progressbar--progress-bar">ProgressBar</a>.
`--radius-circle` can be used for circular elements like
<a href="../?path=/docs/components-radiogroup--radio-group">Radio</a> options or
the "pip" in
<a href="../?path=/docs/components-switch--switch-story">Switch</a>.

export function Example({ of: radius }) {
  const style = {
    width: "var(--space-large)",
    height: "var(--space-large)",
    backgroundColor: "var(--color-indigo)",
    borderRadius: `var(--radius-${radius})`,
  };
  return <div style={style} />;
}

| Radius            | Visual                  |
| :---------------- | :---------------------- |
| `--radius-base`   | <Example of="base" />   |
| `--radius-large`  | <Example of="large" />  |
| `--radius-larger` | <Example of="larger" /> |
| `--radius-circle` | <Example of="circle" /> |
