import { Meta } from "@storybook/addon-docs";

<Meta title="Design/Radii" />

# Border Radius

This base radius can be found in almost all of our components that have a
defined "edge": [Button](../?path=/docs/components-actions-button-docs--page),
[Card](../?path=/docs/components-layouts-and-structure-card-docs--page),
[InputText](../?path=/docs/components-forms-and-inputs-inputtext-docs--page),
and [Menu](../?path=/docs/components-navigation-menu--menu) are all examples of
this in practice.

If an element is nested directly inside of a container with the base radius, or
is itself smaller than the base radius, the small radius may be applied.

Larger rounded corners should be used sparingly, but are available for cases
where the user may expect a smoother corner, such as
[ProgressBar](../?path=/docs/components-status-and-feedback-progressbar-docs--page).
`--radius-circle` can be used for circular elements like
[Radio](../?path=/docs/components-forms-and-inputs-radiogroup--radio-group)
options or the "pip" in
[Switch](../?path=/docs/components-selections-switch--switch-story).

export function Example({ of: radius }) {
  const style = {
    width: "var(--space-largest)",
    height: "var(--space-largest)",
    backgroundColor: "var(--color-indigo)",
    borderRadius: `var(--radius-${radius})`,
  };
  return <div style={style} />;
}

| Radius            | Visual                  |
| :---------------- | :---------------------- |
| `--radius-small`  | <Example of="small" />  |
| `--radius-base`   | <Example of="base" />   |
| `--radius-large`  | <Example of="large" />  |
| `--radius-larger` | <Example of="larger" /> |
| `--radius-circle` | <Example of="circle" /> |
