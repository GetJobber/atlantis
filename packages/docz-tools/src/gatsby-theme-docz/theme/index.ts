import * as colors from "./colors";
import { space } from "./space";
import { fontSizes, fonts } from "./typography";

export default {
  colors,
  fonts,
  fontSizes,
  space,
  styles: {
    root: {
      fontFamily: "body",
      fontSize: "large",
    },
  },
};
