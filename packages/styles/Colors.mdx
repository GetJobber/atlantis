---
name: Custom CSS Properties
menu: Style
route: /style/custom-css-properties
---

import { PropsTable } from "docz";
import { customProperties } from "./lib/custom-properties";

# 🔱 Atlantis Colors

<dl>
  {Object.entries(customProperties).map(([k, v]) => (
    <React.Fragment key={k}>
      <dt>
        <pre>{k}</pre>
      </dt>
      <dd>
        <figure>
          <svg height="7ch" width="7ch" viewBox="0 0 1 1">
            <rect x="0" y="0" height="1" width="1" fill={v} />
          </svg>
          <figcaption style={{ color: v }}>{v}</figcaption>
        </figure>
      </dd>
    </React.Fragment>
  ))}
</dl>
