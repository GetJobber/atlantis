import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Text } from "@jobber/components/Text";
import { Content } from "@jobber/components/Content";
import * as hooks from ".";

<Meta title="Hooks/useInView" />

# useInView

Allows you to detect if an element is visible on the screen.

```ts
import { useInView } from "@jobber/hooks/useInView";
```

<Canvas>
  <Story name="useInView">
    {() => {
      const [ref, isInView] = hooks.useInView();
      return (
        <Content>
          <Text>{isInView ? "A wild donut appeared!" : "Scroll down"}</Text>
          <div style={{ height: 200, overflow: "auto" }}>
            <Content spacing="small">
              {[...Array(10).keys()].map(i => (
                <div
                  key={i}
                  style={{
                    height: "var(--space-larger)",
                    background: "var(--color-grey--lighter)",
                  }}
                />
              ))}
              <div ref={ref}>
                <Text size="large">🍩🍩🍩🍩🍩🍩🍩🍩🍩</Text>
              </div>
            </Content>
          </div>
        </Content>
      );
    }}
  </Story>
</Canvas>

<ArgsTable of={hooks.useInView} />
