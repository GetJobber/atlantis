---
name: useResizeObserver
menu: Hooks
route: /hooks/use-resize-observer
---

# useResizeObserver

## Example

```
import { useResizeObserver } from "@jobber/hooks";

function MyResizeObservedComponent() {
  const [ref, {width, height}] = useResizeObserver();
}
```
