import { defineMessages } from "react-intl";

export const messages = defineMessages({
  time: {
    id: "timeLabel",
    defaultMessage: "Time",
    description: "Label for DateTimePicker with time variant",
  },
});
