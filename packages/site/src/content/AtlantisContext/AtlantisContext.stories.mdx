import { AtlantisContext } from "@jobber/components/AtlantisContext";
import { Meta } from "@storybook/addon-docs";

<Meta title="Components/Contexts/AtlantisContext" component={AtlantisContext} />

# AtlantisContext

Provides global configuration for Atlantis components, including date and time
formats, locale, currency symbols, and other regional settings.

On its own, it has no visual representation. It is purely functional and
expressed through components like InputDate/DatePicker.
