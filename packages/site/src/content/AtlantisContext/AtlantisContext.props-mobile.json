[
  {
    "tags": {},
    "filePath": "../components-native/src/AtlantisContext/AtlantisContext.tsx",
    "description": "",
    "displayName": "AtlantisContext",
    "methods": [],
    "props": {
      "dateFormat": {
        "defaultValue": {
          "value": "\"PP\""
        },
        "description": "The date format Atlantis components would use",
        "name": "dateFormat",
        "required": true,
        "type": {
          "name": "string"
        }
      },
      "timeFormat": {
        "defaultValue": {
          "value": "\"p\""
        },
        "description": "The time format Atlantis components would use",
        "name": "timeFormat",
        "required": true,
        "type": {
          "name": "string"
        }
      },
      "timeZone": {
        "defaultValue": {
          "value": "Intl.DateTimeFormat().resolvedOptions().timeZone"
        },
        "description": "Time zone used in converting the date and time formats",
        "name": "timeZone",
        "required": true,
        "type": {
          "name": "string"
        }
      },
      "isOnline": {
        "defaultValue": {
          "value": "true"
        },
        "description": "Determines how the component behaves when the app is online or offline. This is set to `true` by default. If your usage doesn't require a different experience, you can leave this as-is.",
        "name": "isOnline",
        "required": true,
        "type": {
          "name": "boolean"
        }
      },
      "onLogError": {
        "defaultValue": null,
        "description": "Log errors to rollbar",
        "name": "onLogError",
        "required": true,
        "type": {
          "name": "(message: string) => void"
        }
      },
      "floatSeparators": {
        "defaultValue": {
          "value": "{ group: \",\", decimal: \".\" }"
        },
        "description": "Grabs the decimal separator and group separator based on locale",
        "name": "floatSeparators",
        "required": true,
        "type": {
          "name": "Record<\"decimal\" | \"group\", string>"
        }
      },
      "currencySymbol": {
        "defaultValue": {
          "value": "\"$\""
        },
        "description": "The currency symbol Atlantis components will use",
        "name": "currencySymbol",
        "required": true,
        "type": {
          "name": "string"
        }
      },
      "headerHeight": {
        "defaultValue": {
          "value": "0"
        },
        "description": "The `headerHeight` property represents the height of the app header in Atlantis. It plays a crucial role in determining the positioning of various elements within the app. By accurately defining this value, Atlantis can effectively calculate the layout and alignment of its components.",
        "name": "headerHeight",
        "required": true,
        "type": {
          "name": "number"
        }
      },
      "locale": {
        "defaultValue": {
          "value": "\"en\""
        },
        "description": "Change the locale of the components. This updates the strings that comes with the components, updates the date and time formats, and/or the native 3rd-party packages.",
        "name": "locale",
        "required": true,
        "type": {
          "name": "string"
        }
      },
      "setHeaderHeight": {
        "defaultValue": null,
        "description": "The `setHeaderHeight` method allows you to set the height of the app header in Atlantis. Adjusting this height is essential for ensuring the correct positioning and alignment of various elements within the app. By setting this value accurately, Atlantis can dynamically adjust the layout of its components based on the specified header height.",
        "name": "setHeaderHeight",
        "required": true,
        "type": {
          "name": "(height: number) => void"
        }
      }
    }
  }
]
