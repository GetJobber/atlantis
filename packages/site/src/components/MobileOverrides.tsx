import { View } from "react-native";

export const Touchable = () => ({});
export const Switch = () => ({});
export const Gesture = {
  Native: () => [],
};
export const GestureDetector = props => <View {...props} />;
export const ScrollView = () => ({});
export const Modalize = () => ({});
