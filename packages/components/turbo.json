{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "bootstrap": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "!tsconfig.tsbuildinfo"
      ],
      "outputs": ["dist/**"]
    }
  }
}
