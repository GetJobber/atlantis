---
name: InputPassword
menu: Components
route: /components/input-password
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { useState } from "react";
import { InputPassword } from ".";

# Input Password

<ComponentStatus stage="ready" responsive="yes" accessible="yes" />

InputPassword is used to display a secure password input field.

```ts
import { InputPassword } from "@jobber/components/InputPassword";
```

<Playground>
  <InputPassword defaultValue="password" placeholder="Password" />
</Playground>

## Props

<Props of={InputPassword} />

---

## Controlled

<Playground>
  {() => {
    const [value, setValue] = useState("password");
    return (
      <InputPassword value={value} onChange={setValue} placeholder="Password" />
    );
  }}
</Playground>

## With Error

<Playground>
  {() => {
    const [value, setValue] = useState("password");
    return (
      <InputPassword
        value={value}
        onChange={setValue}
        placeholder="Password"
        name="password"
        validations={{
          required: {
            value: true,
            message: "You need a password."
          },
          minLength: {
            value: 10,
            message: "Passwords should be at least 10 characters."
          }
        }}
      />
    );
  }}
</Playground>
