import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { useState } from "react";
import { InputPassword } from "@jobber/components/InputPassword";

<Meta title="Components/InputPassword" component={InputPassword} />

# Input Password

InputPassword is used to display a secure password input field.

```ts
import { InputPassword } from "@jobber/components/InputPassword";
```

<Canvas>
  <Story
    name="InputPassword"
    args={{
      defaultValue: "password",
      placeholder: "Password",
    }}
  >
    {args => <InputPassword {...args} />}
  </Story>
</Canvas>

## Design & usage guidelines

Use InputPassword for authentication flows where the user is required to enter a
password.

If you are building an experience for authentication that does not require
character-masking, consider using
[InputText](https://atlantis.getjobber.com/components/input-text).

## Props

<ArgsTable of={InputPassword} story="InputPassword" />

---

## Controlled

<Canvas>
  <Story name="Controlled">
    {() => {
      const [value, setValue] = useState("password");
      return (
        <InputPassword
          value={value}
          onChange={setValue}
          placeholder="Password"
        />
      );
    }}
  </Story>
</Canvas>

## With visibility

<Canvas>
  <InputPassword defaultValue="password" placeholder="Password" hasVisibility />
</Canvas>

## With error

For more information about available `validations`, see the
[InputText](/components/input-text/#validation-message) docs.

<Canvas>
  <InputPassword
    placeholder="Password"
    name="password"
    validations={{
      required: {
        value: true,
        message: "You need a password.",
      },
      minLength: {
        value: 10,
        message: "Passwords should be at least 10 characters.",
      },
    }}
  />
</Canvas>
