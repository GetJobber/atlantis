/**
 * BRING INTO JOBBER
 *
 * 1. Create PR :trident:
 *  1.1 When you prerelease, it will release your latest commit
 * 2. npm run prerelease :trident:
 * 3. load your local jobber :octopus:
 * 4. npm install @jobber/components@next @jobber/design/@next :octopus:
 * 5. npm run webpack :octopus:
 *  5.1 will tell you if there are any typescript errors
 * 6. npm run jest :octopus:
 *
 *
 * feat: add cool componet (PR#)
 * @jobber/components@canary v-canary-123456
 */

/*
  align?: "center" | "right";
  defaultValue?: string;
  disabled?: boolean;
  invalid?: boolean;
  inline?: boolean;
  loading?: boolean;
  name?: string;
  onChange?(newValue: string | number): void;
  onValidation?(message: string): void;
  placeholder?: string;
  size?: "small" | "large";
  value?: string | number;
*/
