---
name: Button
menu: Components
route: /components/button
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { Button } from ".";

# Button

<ComponentStatus stage="pre" responsive="no" accessible="no" />

Buttons are a core user interface component, as they allow users to initiate,
complete, and reverse actions. Button classes can be applied to a number of HTML
elements button, anchor, or submit inputs. We should always use semantic
elements for buttons in favour of things like spans or divs so we get
accessibility out of the box.

<Playground>
  <Button label="Submit" />
  <Button label="Submit" variation="secondary" />
  <Button label="Submit" variation="tertiary" />
  <br />
  <Button label="Learn More" interactionType="learning" />
  <Button label="Learn More" interactionType="learning" variation="secondary" />
  <Button label="Learn More" interactionType="learning" variation="tertiary" />
  <br />
  <Button label="Delete" interactionType="destructive" />
  <Button label="Delete" interactionType="destructive" variation="secondary" />
  <br />
  <Button label="Cancel" interactionType="cancel" />
</Playground>

## Button Properties

<Props of={Button} />
