---
name: Select
menu: Components
route: /components/select
showDirectoryLink: true
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { Select, Option } from ".";

# Select

<ComponentStatus stage="ready" responsive="yes" accessible="yes" />

In Jobber a `Select` is used to present a defined list of options for the user
to choose from.

```ts
import { Select, Option } from "@jobber/components/Select";
```

<Playground>
  <Select placeholder="My best friend">
    <Option value="tony">Tony</Option>
    <Option value="quincy">Quincy</Option>
    <Option value="peppa">Peppa Pig</Option>
  </Select>
</Playground>

## Props

<Props of={Select} />

---

## Sizes

### Small

<Playground>
  <Select size="small">
    <Option value="">Small</Option>
    <Option value="sad" disabled>
      Tony
    </Option>
    <Option value="old">Steve</Option>
    <Option value="splat">Natasha</Option>
  </Select>
</Playground>

### Large

<Playground>
  <Select size="large">
    <Option value="">Small</Option>
    <Option value="sad" disabled>
      Tony
    </Option>
    <Option value="old">Steve</Option>
    <Option value="splat">Natasha</Option>
  </Select>
</Playground>

## States

### Invalid

<Playground>
  <Select size="large" invalid={true}>
    <Option value="sad">Tony</Option>
    <Option value="old">Steve</Option>
  </Select>
</Playground>

### Disabled

<Playground>
  <Select size="large" disabled={true}>
    <Option value="sad">Tony</Option>
    <Option value="old">Steve</Option>
  </Select>
</Playground>

## Initial value

<Playground>
  <Select defaultValue="yoink">
    <Option value="sad">Tony</Option>
    <Option value="old">Steve</Option>
    <Option value="splat">Natasha</Option>
    <Option value="yoink">Vision</Option>
  </Select>
</Playground>

## Events

<Playground>
  <Select onChange={x => console.log(x)}>
    <Option value="sad">Tony</Option>
    <Option value="old">Steve</Option>
  </Select>
</Playground>

---

## ▸ `Option`

An `Option` defines a value selectable by a `<Select>`.

## Option props

<Props of={Option} />
