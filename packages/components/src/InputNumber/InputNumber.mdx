---
name: InputNumber
menu: Components
route: /components/input-number
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { InputNumber } from ".";
import { useState } from "react";

# InputNumber

<ComponentStatus stage="pre" responsive="no" accessible="no" />

In Jobber a InputNumber is used to …

## Simple Example

<Playground>
  {() => {
    const [value, setValue] = useState(1234567890);
    const handleChange = newValue => {
      setValue(newValue);
    };
    return (
      <InputNumber value={value} onChange={handleChange} placeholder="Age" />
    );
  }}
</Playground>

## InputNumber Properties

<Props of={InputNumber} />
