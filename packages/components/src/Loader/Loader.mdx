---
name: Loader
menu: Components
route: /components/loader
showDirectoryLink: true
---

import { Playground, Props } from "docz";
import { useState } from "react";
import { Button } from "../Button";
import { Loader } from ".";
import { Card } from "../Card";
import { Text } from "../Text";
import { Content } from "../Content";
import { Checkbox } from "../Checkbox";

# Loader

The `Loader` component will provide the user with feedback that a portion of the
interface is fetching new data or content. It will keep the user informed of
system status as well as help the user avoid mistakes by preventing the user
from interacting with this area of the interface until the new data or content
is ready.

```ts
import { Loader } from "@jobber/components/Loader";
```

<Playground>
  <Loader loading>
    <Card title="Loading Content">
      <Content>
        <Text>
          Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
          eiusmod tempor incididunt ut labore et dolore magna aliqua.
        </Text>
      </Content>
    </Card>
  </Loader>
</Playground>

## Props

<Props of={Loader} />

## Design & Usage Guidelines

### Loading

<Playground>
  {() => {
    const [isLoading, setIsLoading] = useState(false);
    return (
      <>
        <button onClick={() => setIsLoading(!isLoading)}>Toggle Loading</button>
        <Loader loading={isLoading}>
          <Card title="Loading Content">
            <Content>
              <Text>
                Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
                eiusmod tempor incididunt ut labore et dolore magna aliqua.
              </Text>
            </Content>
          </Card>
        </Loader>
      </>
    );
  }}
</Playground>

### Inline

<Playground>
  <Loader loading inline>
    <Card title="Loading Content">
      <Content>
        <Text>
          Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
          eiusmod tempor incididunt ut labore et dolore magna aliqua.
        </Text>
      </Content>
    </Card>
  </Loader>
</Playground>

### Determinate

<Playground>
  {() => {
    const [isLoading, setIsLoading] = useState(false);
    const [currentValue, setCurrentValue] = useState(0);
    return (
      <Content>
        <Checkbox label="Loading" checked={isLoading} onChange={setIsLoading} />
        <Button
          label="Increase Loading Progress"
          size="small"
          icon="add"
          disabled={!isLoading}
          onClick={() => setCurrentValue(Math.min(currentValue + 10, 100))}
        />
        <Button
          label="Decrease Loading Progress"
          size="small"
          icon="minus2"
          disabled={!isLoading}
          onClick={() => setCurrentValue(Math.max(currentValue - 10, 0))}
        />
        <Loader
          determinate
          loading={isLoading}
          currentValue={currentValue}
          maxValue={100}
        >
          <Card title="Loading Content">
            <Content>
              <Text>
                Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
                eiusmod tempor incididunt ut labore et dolore magna aliqua.
              </Text>
            </Content>
          </Card>
        </Loader>
      </Content>
    );
  }}
</Playground>
