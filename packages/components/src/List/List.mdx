---
name: List
menu: Components
route: /components/list
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { List } from ".";
import { Card } from "../Card";
import { useState } from "react";

# List

<ComponentStatus stage="pre" responsive="no" accessible="no" />

Write a description about what List component is trying to achieve.

```ts
import { List } from "@jobber/components/List";
```

<Playground>
  {() => {
    const initialData = [
      {
        id: 2,
        icon: "gift",
        content: "Nullam id dolor id nibh ultricies vehicula ut id elit. Fusce dapibus, tellus.",
        date: "Sep 24, 2019",
        onClick: () => alert("🚀"),
        section: "Yesterday",
      },
    ];

    const [items, setItems] = useState(initialData);
    return (
      <>
        <button onClick={handleClick}>Click me</button>
        <Card>
          <List items={items} />
        </Card>
      </>
    );

    function handleClick() {
      setItems([{
        id: Math.random(),
        icon: "gift",
        content: `Nullam id dolor id nibh ultricies vehicula ut id elit. Fusce dapibus, tellus.`,
        date: "Sep 24, 2019",
        onClick: () => alert("🚀"),
        section: "Today",
      }, ...items ]);
    }

}}

</Playground>

## Props

<Props of={List} />
