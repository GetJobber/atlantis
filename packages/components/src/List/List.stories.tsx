import React from "react";
import { ListItemProps } from "./ListItem";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const StorybookListPropsInterface = (_: ListItemProps): JSX.Element => (
  <></>
);
