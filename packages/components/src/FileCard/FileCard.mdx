---
name: FileCard
menu: Components
route: /components/file-card
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { FileCard } from ".";

# File Card

<ComponentStatus stage="pre" responsive="no" accessible="no" />

Write a description about what FileCard component is trying to achieve.

```ts
import { FileCard } from "@jobber/components/FileCard";
```

<Playground>
  <FileCard
    file={{
      key: "abc",
      name: "myballisbigandroundIamrollingitontheground.png",
      type: "image/png",
      size: 213402324,
      progress: 0.3,
      src: () =>
        Promise.resolve(
          "https://res-1.cloudinary.com/crunchbase-production/image/upload/c_thumb,h_256,w_256,f_auto,g_faces,z_0.7,q_auto:eco/ywedismk5vf5uqc2nngg"
        )
    }}
    onDelete={() => alert("🧨BALEETED!💥")}
  />
  <FileCard
    file={{
      key: "mosdef",
      name: "tps_report.png",
      type: "application/pdf",
      size: 2134023,
      progress: 1
    }}
    onDelete={() => alert("🧨BALEETED!💥")}
  />
</Playground>

<!--
  It is not necessary to include Design & Usage Guidlelines. Use this section
  to answer common questions and considerations about component usage.
-->

## Design & Usage Guidelines

When contributing to, or consuming the FileCard component, consider the
following:

- When using the FileCard component always consider the volume you would like it
  to be displayed at.
- The FileCard component should never be used inside another component.

## Props

<Props of={FileCard} />

<!--
  It is not necessary to provide an example of each prop. Use usage examples to
  highlight key features of a component or particular considerations.

  See `Button.mdx` for a good example of this.
-->
