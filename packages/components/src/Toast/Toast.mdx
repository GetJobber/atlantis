---
name: Toast
menu: Components
route: /components/toast
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { useRef } from "react";
import { Button } from "@jobber/components/Button";
import { Toast } from ".";
import { Slice } from "./Toast";

# Toast

<ComponentStatus stage="ready" responsive="no" accessible="no" />

Toasts are short, temporary messages used to inform users that a process was
performed. They provide visual feedback on the outcome of an action, and require
minimal user interacction.

```ts
import { Toast, ToastRef } from "@jobber/components/Toast";
```

<Playground>
  {() => {
    const toastRef = useRef();
    const buttons = [
      {
        label: "No Variation",
        variation: "cancel",
        onClick: () => {
          toastRef.current.add({
            message: "Bland Toast",
            onClose: () => console.log("🍞")
          });
        }
      },
      {
        label: "Success",
        onClick: () => {
          toastRef.current.add({
            message: "Toast with jelly",
            variation: "success",
            action: () => alert("✅"),
            actionLabel: "Yumm",
            onClose: () => console.log("✅")
          });
        }
      },
      {
        label: "Error",
        variation: "destructive",
        onClick: () => {
          toastRef.current.add({
            message: "Aww dang, you burnt the toast",
            variation: "error",
            onClose: () => console.log("🚨")
          });
        }
      }
    ];
    return (
      <>
        {buttons.map(button => (
          <Button key={button.label} {...button} />
        ))}
        <Toast ref={toastRef} />
      </>
    );
  }}
</Playground>

## Design & Usage Guidelines

When contributing to, or consuming the Toast component, consider the following:

- Keep text labels as short as possible. They should be clear and concise, and
  should not take up more than one line.
- Use the pattern: noun + verb.
  - Examples: `Job created`, `Quote approved`, `Client archived`.
- No need to use "Successfully", should be implicit in the toast.
- Don't use `Dismiss` or `Cancel` as an action label.
  - Examples of action labels: `Undo`, `View`, `Refresh`.
- Only one `<Toast />` should be used per view.

## Adding Toasts

Toast exposes an `add` method when using the `ToastRef` that allows you to add
items into a single `<Toast />` component.

```ts
const toastRef = useRef<ToastRef>();
```

<Playground>
  {() => {
    const toastRef = useRef();
    return (
      <>
        <Button
          label="Add Toast"
          onClick={() => {
            toastRef.current.add({
              message: "New Toast"
            });
          }}
        />
        <Toast ref={toastRef} />
      </>
    );
  }}
</Playground>

## "Add" Method Props

<Props of={Slice} />
