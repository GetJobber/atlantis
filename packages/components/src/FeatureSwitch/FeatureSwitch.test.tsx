// import React from "react";
// import renderer from "react-test-renderer";
// import { cleanup, fireEvent, render } from "@testing-library/react";
// import { FeatureSwitch } from ".";

// afterEach(cleanup);

it("renders a FeatureSwitch", () => {
  // const tree = renderer.create(<FeatureSwitch text="Foo" />).toJSON();
  // expect(tree).toMatchInlineSnapshot();
});

it("renders a loud FeatureSwitch", () => {
  // const tree = renderer.create(<FeatureSwitch text="Foo" loud />).toJSON();
  // expect(tree).toMatchInlineSnapshot();
});

test("it should call the handler with the new value", () => {
  // const changeHandler = jest.fn();
  // const newValue = "Bar";
  // const { getByLabelText } = render(
  //   <FeatureSwitch onChange={changeHandler} placeholder={placeholder} />,
  // );
  // fireEvent.change(getByLabelText(placeholder), {
  //   target: { value: newValue },
  // });
  // expect(changeHandler).toHaveBeenCalledWith(newValue);
});
