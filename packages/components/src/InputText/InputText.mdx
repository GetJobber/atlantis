---
name: InputText
menu: Components
route: /components/input-text
showDirectoryLink: true
---

import { Playground, Props } from "docz";
import { useState } from "react";
import { ComponentStatus } from "@jobber/docx";
import { InputText } from "@jobber/components/InputText";
import { Button } from "@jobber/components/Button";
import { Content } from "@jobber/components/Content";
import { Text } from "@jobber/components/Text";

# Input Text

<ComponentStatus stage="ready" responsive="yes" accessible="yes" />

Input text is used in forms that accept short or long answers from users.

```ts
import { InputText } from "@jobber/components/InputText";
```

<Playground>
  <InputText placeholder="Ship Name" defaultValue="Rocinante" />

  <InputText
    placeholder="Ship Name"
    defaultValue="A really really really long value so that we can see if the padding for the suffix is correct. We can only tell with a really long value"
    suffix={{ label: "Goodbye" }}
  />
  <InputText
    placeholder="Ship Name"
    defaultValue="A really really really long value so that we can see if the padding for the suffix is correct. We can only tell with a really long value"
    suffix={{ icon: "job", label: "Goodbye" }}
  />
  <InputText
    placeholder="Ship Name"
    defaultValue="A really really really long value so that we can see if the padding for the suffix is correct. We can only tell with a really long value"
    suffix={{ icon: "job", label: "Goodbye", onClick: () => alert("Hello") }}
  />
  <InputText
    placeholder="Ship Name"
    defaultValue="A really really really long value so that we can see if the padding for the suffix is correct. We can only tell with a really long value"
    suffix={{ icon: "job", onClick: () => alert("Hello") }}
  />
  <InputText
    placeholder="Ship Name"
    defaultValue="A really really really long value so that we can see if the padding for the suffix is correct. We can only tell with a really long value"
    suffix={{ icon: "gift" }}
  />
  <InputText
    placeholder="Ship Name"
    defaultValue="Rocinante"
    prefix={{ icon: "home" }}
  />
  <InputText
    placeholder="Ship Name"
    defaultValue="Rocinante"
    prefix={{ label: "Hello with a longer prefix", icon: "gift" }}
  />
  <InputText
    placeholder="Ship Name"
    defaultValue="Rocinante"
    prefix={{ label: "Hello with a longer prefix" }}
  />
</Playground>
