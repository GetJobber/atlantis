---
name: InputText
menu: Components
route: /components/input-text
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { InputText } from ".";
import { Text } from "../Text";

# Input Text

<ComponentStatus stage="rc" responsive="yes" accessible="yes" />

Input Text is used in forms to accept short answers from users.

<Playground>
  <InputText name="invoiceSubject" placeholder="Invoice subject" />
</Playground>

## States

<Playground>
  <InputText name="disabled" disabled={true} placeholder="Disabled input" />
  <InputText
    name="readOnly"
    readonly={true}
    placeholder="Readonly input"
    value="You can't change me."
  />
  <InputText name="invalid" invalid={true} placeholder="Invalid input" />
</Playground>

## Sizes

<Playground>
  <InputText name="smallField" size="small" placeholder="Duration" />
  <InputText name="largeField" size="large" placeholder="Your username" />
</Playground>

## Inline

<Playground>
  <Text>
    Send a follow-up
    <InputText
      name="durationInDays"
      value="2"
      size="small"
      inline={true}
    /> days after
  </Text>
</Playground>

## Properties

<Props of={InputText} />
