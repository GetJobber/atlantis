---
name: Countdown
menu: Components
route: /components/countdown
showDirectoryLink: true
---

import { Playground, Props } from "docz";
import { Countdown } from ".";
import { useMemo } from "react";

# Countdown

```ts
import { Countdown } from "@jobber/components/Countdown";
```

<Playground>
  {() => {
    const inFiveMinutes = useMemo(
      () => new Date(new Date().getTime() + 25 * 3600 * 1000),
      []
    );
    return (
      <Countdown date={inFiveMinutes} granularity={"dhms"} showUnits={true} />
    );
  }}
</Playground>

## Props

<Props of={Countdown} />

## On Complete

<Playground>
  {() => {
    const inTenSeconds = useMemo(
      () => new Date(new Date().getTime() + 10 * 1000),
      []
    );
    return (
      <Countdown
        date={inTenSeconds}
        granularity={"hdms"}
        showUnits={true}
        onComplete={() => alert("Completed Timer!")}
      />
    );
  }}
</Playground>
