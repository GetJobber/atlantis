---
name: Checkbox
menu: Components
route: /components/checkbox
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { Checkbox } from ".";
import { useState } from "react";

# Checkbox

<ComponentStatus stage="pre" responsive="no" accessible="no" />

Checkboxes let a user select one or more items from a set of options.

```ts
import { Checkbox } from "@jobber/components/Checkbox";
```

<Playground>
  {() => {
    const [checked, setChecked] = useState(true);

    return <Checkbox checked={checked} label="Save card for future use" onClick={setChecked} />

}}

</Playground>

## Props

<Props of={Checkbox} />

---

## Indeterminate

<Playground>
  <Checkbox checked={true} label="2 selected" indeterminate={true} /> <br />
  <Checkbox checked={true} label="Item 1" /> <br />
  <Checkbox checked={true} label="Item 2" /> <br />
  <Checkbox checked={false} label="Item 3" />
</Playground>

## Disabled

<Playground>
  <Checkbox
    disabled={true}
    checked={true}
    label="Subscribe on marketing emails"
  />
</Playground>
