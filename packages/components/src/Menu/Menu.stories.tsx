import React from "react";
import { SectionProps } from ".";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const StorybookMenuSectionShape = (_: SectionProps): JSX.Element => (
  <></>
);
