---
name: Menu
menu: Components
route: /components/menu
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { Menu, SectionProps } from ".";

# Menu

<ComponentStatus stage="pre" responsive="no" accessible="yes" />

A toggleable menu that holds a list of actions.

<Playground>
  <Menu
    items={[
      {
        actions: [
          {
            label: "Edit",
            icon: "edit",
            onClick: () => {
              alert("✏️");
            }
          }
        ]
      },
      {
        header: "Send as...",
        actions: [
          {
            label: "Text Message",
            icon: "sms",
            onClick: () => {
              alert("📱");
            }
          },
          {
            label: "Email",
            icon: "email",
            onClick: () => {
              alert("📨");
            }
          }
        ]
      },
      {
        header: "Mark as...",
        actions: [
          {
            label: "Sent",
            icon: "sendInvoice",
            onClick: () => {
              alert("💌");
            }
          },
          {
            label: "Paid",
            icon: "paidInvoice",
            onClick: () => {
              alert("💳");
            }
          }
        ]
      }
    ]}
  />
</Playground>

## Menu Properties

<Props of={Menu} />
