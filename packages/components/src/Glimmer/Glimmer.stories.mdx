import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Banner } from "@jobber/components/Banner";
import { Card } from "@jobber/components/Card";
import { Content } from "@jobber/components/Content";
import { Glimmer } from ".";

<Meta title="Components/Glimmer" component={Glimmer} />

# Glimmer

<Banner type="warning" dismissible={false}>

**Experimental component**. This component is subject to change and will have
**breaking changes**, use at your own risk.

</Banner>

Glimmer is a foundational component that is used to build `<Skeleton />`
(_coming soon_) or other components that invoke loading such as an image waiting
to load on [FormatFile](/docs/components-formatfile--format-file).

```ts
import { Glimmer } from "@jobber/components/Glimmer";
```

<Canvas>
  <Story name="Glimmer">
    {args => (
      <Card>
        <Content>
          <div
            style={{
              display: "grid",
              gridTemplateColumns: "max-content minmax(auto, 30%)",
              gap: "var(--space-base)",
              alignItems: "center",
            }}
          >
            <Glimmer size="larger" shape="circle" {...args} />
            <Content spacing="small">
              <Glimmer size="large" {...args} />
              <Glimmer size="small" shape="rectangleShorter" {...args} />
            </Content>
          </div>
        </Content>
        <Content>
          <Content spacing="small">
            <Glimmer size="small" {...args} />
            <Glimmer size="small" shape="rectangleShort" {...args} />
            <Glimmer size="small" shape="rectangleShorter" {...args} />
          </Content>
          <div style={{ display: "flex", gap: "var(--space-base)" }}>
            <Glimmer size="largest" shape="square" {...args} />
            <Glimmer size="largest" shape="square" {...args} />
            <Glimmer size="largest" shape="square" {...args} />
            <Glimmer size="largest" shape="square" {...args} />
          </div>
        </Content>
      </Card>
    )}
  </Story>
</Canvas>

<ArgsTable of={Glimmer} story="Glimmer" />
