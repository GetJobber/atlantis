---
name: Drawer
menu: Components
route: /components/drawer
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { useState } from "react";

import { Drawer, DrawerGrid } from ".";
import { Content } from "@jobber/components/Content";
import { Button } from "@jobber/components/Button";
import { Text } from "../Text";
import { Page } from "../Page";

#Drawer

<ComponentStatus stage="pre" responsive="no" accessible="yes" />

Drawer is a UI element meant to store content in a sidebar with a visibility
toggle.

```ts
import { Page } from "@jobber/components/Page";
```

<Playground>
  {() => {
    const [modalOpen, setModalOpen] = useState(false);
    return(
      <Page
        title="Notifications"
        intro=""
      >
      <DrawerGrid>
        <Content>
          <Button onClick={() => setModalOpen(!modalOpen)} label="Toggle Drawer"></Button>
        </Content>
        <Drawer title="me drawer" open={modalOpen}></Drawer>
      </DrawerGrid>

      <Text>Page content here</Text>

  </Page>)
  }}

</Playground>
