---
name: ConfirmationModal
menu: Components
route: /components/confirmation-modal
---

import { useRef, useState } from "react";
import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { ConfirmationModal } from ".";

# Confirmation Modal

<ComponentStatus stage="pre" responsive="no" accessible="no" />

Write a description about what ConfirmationModal component is trying to achieve.

```ts
import {
  ConfirmationModal,
  confirmationModalReducer
} from "@jobber/components/ConfirmationModal";
```

<Playground>
  {() => {
    const confirmationModalRef = useRef();
    const users = [
      {
        id: 1,
        name: "Bob"
      },
      {
        id: 2,
        name: "Donald"
      }
    ];
    return (
      <>
        {users.map(user => {
          return (
            <button
              onClick={() => {
                confirmationModalRef.current.show();
              }}
            >
              Alert {user.name}
            </button>
          );
        })}
        <ConfirmationModal ref={confirmationModalRef} />
      </>
    );
  }}
</Playground>

<!--
  It is not necessary to include Design & Usage Guidlelines. Use this section
  to answer common questions and considerations about component usage.
-->

## Design & Usage Guidelines

When contributing to, or consuming the ConfirmationModal component, consider the
following:

- When using the ConfirmationModal component always consider the volume you
  would like it to be displayed at.
- The ConfirmationModal component should never be used inside another component.

## Props

<Props of={ConfirmationModal} />

<!--
  It is not necessary to provide an example of each prop. Use usage examples to
  highlight key features of a component or particular considerations.

  See `Button.mdx` for a good example of this.
-->

## With state

<Playground>
  {() => {
    const [open, setOpen] = useState(false);
    return (
      <>
        <button onClick={() => setOpen(true)}>Open</button>
        <ConfirmationModal
          state={{
            title: "Using State",
            open: open
          }}
          onConfirm={() => {
            alert("okay then");
            setOpen(false);
          }}
          onCancel={() => setOpen(false)}
        />
      </>
    );
  }}
</Playground>
