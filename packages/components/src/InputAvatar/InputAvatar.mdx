---
name: InputAvatar
menu: Components
route: /components/input-avatar
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { useState } from "react";
import { InputAvatar } from ".";

# Input Avatar

<ComponentStatus stage="pre" responsive="no" accessible="no" />

Write a description about what InputAvatar component is trying to achieve.

```ts
import { InputAvatar } from "@jobber/components/InputAvatar";
```

<Playground>
  {() => {
    const [files, setFiles] = useState([]);
    return (
      <InputAvatar
        getUploadParams={() =>
          Promise.resolve({ url: "https://httpbin.org/post" })
        }
        onUploadStart={handleUpload}
        onUploadProgress={handleUpload}
        onUploadComplete={handleUpload}
      />
    );
    function handleUpload(file) {
      setFiles(oldFiles => updateFiles(file, oldFiles));
    }
  }}
</Playground>

<!--
  It is not necessary to include Design & Usage Guidlelines. Use this section
  to answer common questions and considerations about component usage.
-->

## Design & Usage Guidelines

When contributing to, or consuming the InputAvatar component, consider the
following:

- When using the InputAvatar component always consider the volume you would like
  it to be displayed at.
- The InputAvatar component should never be used inside another component.

## Props

<Props of={InputAvatar} />

<!--
  It is not necessary to provide an example of each prop. Use usage examples to
  highlight key features of a component or particular considerations.

  See `Button.mdx` for a good example of this.
-->

## Loud

Use `loud` to indicate that a component is yelling.

<Playground>
  <InputAvatar text="Fred" loud={true} />
</Playground>
