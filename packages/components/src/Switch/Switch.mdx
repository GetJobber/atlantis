---
name: Switch
menu: Components
route: /components/switch
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { useState } from "react";
import { Switch } from ".";

# Switch

<ComponentStatus stage="pre" responsive="yes" accessible="yes" />

A switch toggles the state of a single setting to on or off.

<Playground>
  <Switch ariaLabel="Visible to clients" />
</Playground>

## Parent controlled switch

<Playground>
  {() => {
    const [value, setValue] = useState(true);
    const label = "Toggle example";
    return (
      <>
        <button onClick={() => setValue(!value)}>{label}</button>
        <br />
        <Switch
          value={value}
          onChange={newValue => setValue(newValue)}
          ariaLabel={label}
        />
      </>
    );
  }}
</Playground>

## Disabled

<Playground>
  <Switch value={true} ariaLabel="Visible to clients" disabled={true} />
</Playground>

## Switch Properties

<Props of={Switch} />
