---
name: Switch
menu: Components
route: /components/switch
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { useState } from "react";
import { Switch } from ".";

# Switch

<ComponentStatus stage="pre" responsive="no" accessible="no" />

In Jobber a Switch is used to …

## Simple Example

<Playground>
  <Switch />
  <br />
  <Switch value={true} />
</Playground>

<Playground>
  {() => {
    const [value, setValue] = useState(true);
    return (
      <>
        <button onClick={() => setValue(!value)}>Toggle</button>
        <br />
        <Switch value={value} onChange={newValue => setValue(newValue)} />
      </>
    );
  }}
</Playground>

## Switch Properties

<Props of={Switch} />
