---
name: Modal
menu: Components
route: /components/modal
---

import { Playground, Props } from "docz";
import { ComponentStatus } from "@jobber/docx";
import { useState } from "react";
import { Modal } from ".";

# Modal

<ComponentStatus stage="pre" responsive="no" accessible="no" />

In Jobber a Modal is used to …

## Simple Example

<Playground>
  {() => {
    const [value, setValue] = useState(true);
    return (
      <>
        <button onClick={() => setValue(!value)}>Open Modal</button>
        <Modal title="Bob" open={value} onRequestClose={() => setValue(!value)}>
          Haloo
        </Modal>
      </>
    );
  }}
</Playground>

## Modal Properties

<Props of={Modal} />
