import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Text } from "@jobber/components-native";

<Meta title="Components/Text/Mobile" component={Text} />

# Text

Text is used to add an additional visual meaning on your content

```ts
import { Text } from "@jobber/components-native";
```

<Canvas>
  <Story
    name="Mobile"
    args={{
      children:
        "'640K is more memory than anyone will ever need on a computer' - Not Bill Gates",
    }}
  >
    {args => <Text {...args} />}
  </Story>
</Canvas>

<ArgsTable of={Text} story="Mobile" />
