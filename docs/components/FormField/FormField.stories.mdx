import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { FormField } from "@jobber/components/FormField";

<Meta title="Components/Private/FormField/Docs" component={FormField} />

# Form Field

Master component for all things FormField. The idea is to keep all the styles in
one place.
